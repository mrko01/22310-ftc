{
  "name": "edit-saffron-public-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "export:brief": "npm run build && node scripts/export-brief.mjs",
    "check": "npm run build && node scripts/check-links.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.63.0"
  }
}
