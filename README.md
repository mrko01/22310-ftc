# 22310 FTC

The website for FTC Team 22310 at https://22310.ca.

Static HTML and CSS in `dist/index.html`. Cloudflare Pages deploys the `dist` directory from the `main` branch. No build command or dependencies are required.
